declare module 'qrcode' {
// Define types for the members you're using, e.g.:
    export enum QRCodeErrorCorrectionLevel {
        L = 'L',
        M = 'M',
        Q = 'Q',
        H = 'H'
    }
    // Add more types as needed
}